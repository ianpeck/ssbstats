# Scripts

Standalone maintenance utilities live here so the repo root stays focused on the web app.

- `maintenance/elo.py`: recompute and write Elo ratings, optionally emitting `generated/elo_output.csv`
- `maintenance/power_score.py`: inspect and print power-score rankings from the database
